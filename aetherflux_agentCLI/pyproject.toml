[project]
name = "aetherflux-agentcli"
version = "0.2.7"
description = "AetherFlux V0.2.7 agent-led autonomous collector workflow."
requires-python = ">=3.9"

[project.scripts]
aetherflux-agentcli = "aetherflux_agentcli.cli:main"

[tool.setuptools.packages.find]
include = ["aetherflux_agentcli*"]
