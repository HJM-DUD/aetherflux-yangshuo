[project]
name = "aetherflux-yangshuo"
version = "0.2.0"
description = "Local-first tourism video intelligence collection, review, dashboard, and agent API."
requires-python = ">=3.9"

[tool.aetherflux]
default_host = "127.0.0.1"
default_port = 8788
worker_api_port = 8789
