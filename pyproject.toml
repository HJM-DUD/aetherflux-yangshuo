[project]
name = "aetherflux-yangshuo"
version = "0.1.0"
description = "Yangshuo tourism intelligence collection, review, dashboard, and agent API."
requires-python = ">=3.9"

[tool.aetherflux]
default_host = "127.0.0.1"
default_port = 8765
